# DevLog — 2026-07-10 (영욱)

## 오늘 한 작업

### 1. 오픈 PR 6개 전부 머지 — 오픈 PR 0개
- **PR #30**: MatchRecorder 후속 2건 (판 종료 후 상태 리셋 + 저장 실패 시 이벤트 미발행)
- **PR #31**: 이브이 영웅증강 검증용 디버그 컴포넌트
- **PR #32**: 스킬 VFX 재생 뼈대 (VfxDatabase 레지스트리 + CastSkill 재생 훅)
- **PR #33**: 스킬 가이드 정합 (MANA_REGEN 버프화 + TAUNT 시간제)
- **PR #34 (태욱님)**: 전적창 매치 히스토리 UI — 코드 정합 검토 통과 (MatchRecord 스키마/LoadRecent/이벤트 시그니처 전부 일치)
- ⚠️ **PR #34가 base가 `feature/stage-data`로 열려 있어 master에 반영되지 않는 문제가 있었습니다.** PR #35로 master에 옮겨 머지했습니다. **PR 만드실 때 base가 master인지 꼭 확인 부탁드립니다.**

### 2. 씬 배선 + meta 정리 (master 직커밋 f089a4f9)
- `GameSceneTest`의 GameManager 오브젝트에 `EeveeHeroDebugTest` 부착
- PR #32에서 누락됐던 `VfxDatabase.cs` / `BattleVfxPlayer.cs`의 .meta를 배치모드로 생성·커밋 (Unity 6000.3.8f1 배치 컴파일 에러 0)
- MatchRecorder는 PR #28 때 이미 씬에 부착돼 있었음을 확인

### 3. 이브이 벤치 지급 디버그 버튼 추가 (e4a4bac0)
상점 등장 확률이 낮아 이브이 검증이 어려워, `EeveeHeroDebugTest`에 "⓪ 이브이 벤치 지급" 버튼을 추가했습니다.
- AssetDatabase로 Eevee PokemonData를 찾아 `UnitFactory.Create` → 벤치 배치
- **골드 미차감 + 상점 풀 미차감** (디버그 지급이 풀 카운트를 왜곡하지 않도록), `UNITY_EDITOR` 가드 내라 빌드 미포함

### 4. 에디터 플레이 검증 — 전부 통과 (로그 Exception 0건)
1-5 슬라이스 한 판 완주로 아래를 확인했습니다.
- **이브이 영웅증강**: 증강 적용 → 3성 → 전투에서 추가 이브이(봇) 소환 확인
- **전적 기록**: `matches.jsonl`에 Victory/1-5/R5 정상 기록 — 최종 보드 스냅샷(3성 이브이 4마리 + 돌진화 부스터)까지 스키마대로 저장
- **전적창 UI (PR #34)**: 방금 판 포함 기록 표시 정상 — **태욱님 1순위 작업 실검증 완료입니다** 🎉
- **VFX 재생 훅 (PR #32)**: CastSkill에서 재생 시도까지 실동작 확인. 데이터에 vfxId가 이미 채워져 있어, 미등록 경고 5종이 로그에 남았습니다:
  `VFX_Mutant_Magician` / `VFX_Mutant_Tanker` / `VFX_Mutant_Warrior` / `VFX_Water_Warrior` / `VFX_Dragon_Magician`
  → `Resources/VfxDatabase.asset` 생성 후 이 vfxId들에 프리팹을 연결하면 바로 재생됩니다. (규격: Notion "VFX 연동 규격 안내")

## 머지/배포 상태
- PR #30~35 모두 master 머지 완료. master 최신: `e4a4bac0`
- 받으실 때 base가 master인지 확인 부탁드립니다.

## 다음 작업
- `VfxDatabase.asset` 생성 + vfxId 5종 프리팹 연결 (아트 협업)
- 1R 시작골드 20 확인 (씬 10 + RW001 10 중복 건 — 유지/수정 최종 결정)
- 2인 실테스트: 재접속 / 통신교환 / 증강 3택1 / 골드 전송 엣지 / 전적 endReason별
- 기획 확인 대기 2건: STUN 데미지+스턴 복합 여부, 마나 충전 모델(초당 vs 평타당)
