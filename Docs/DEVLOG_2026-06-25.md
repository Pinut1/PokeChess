# DevLog — 2026-06-25 (영욱)

## 오늘 한 작업

### 1. 시너지 시스템 완성 (전투 적용까지)
그동안 "카운트/티어 표시만 되고 전투엔 효과 없음"이던 시너지를 끝까지 연결했습니다.
- **데이터**: `synergy_data.json` 16종으로 확장(효과 설명에 기획 §6 실수치 반영)
- **중앙 DB화**: `SynergyDatabase`(SO) 신설 → PokemonDatabase/ItemDatabase와 동일 패턴. 임포터가 자동 갱신, `SynergyManager`가 `SynergyDatabase.Instance`에서 자동 로드합니다. **이제 `PokeChess > Import Synergy JSON`만 돌리면 인스펙터 수동 배선 없이 적용됩니다.**
- **전투 버프**: `SynergyConstants`(기획 §6 수치, 코드 상수) + `BattleManager`에서 아군 트레잇 보유 유닛에 적용. 일반 11종 동작.
- **특수 시너지 3종**: 돌연변이(봇 누적 소환)·얼음(적 공속 -20%)·치어리더(공속/마나 선택). 악(첫 스킬 스턴)은 다음 작업.
- **HUD**: 좌측 시너지 특성 패널(롤체식) + 마우스 호버 시 단계별 효과 툴팁 + 치어리더 선택 토글.

### 2. 적 트레이너 보유 아이템 효과 연결
보스/트레이너의 `heldItemEn`이 전투에서 실제로 적용되도록 연결했습니다(기존 누락).

### 3. 태욱님 PR 2건 머지
- #10 상점 풀 확률 추첨 + 아이템 쿠폰
- #11 소모품 보상 지급
- 충돌(ItemManager/RewardManager/씬)은 "양쪽 다 살리기"로 해결, 검증 완료.

## 머지/배포 상태
- 모두 `feature/stage-data`에 푸시 완료. 받으시면 `Import Synergy JSON` 한 번 돌려주세요.

## 팀 공유 / 후속
- **태욱님**: 다음 큰 작업은 **레벨/XP 시스템**입니다. 이미 만들어두신 `GetCostRates(level)` 확률 테이블 + `OnLevelChanged` 구독이 그대로 쓰여서, "레벨을 올리는 주체(XP/구매/라운드XP)"만 추가하면 상점 확률이 자동 연결됩니다.
- **해인님**: 시너지 정식 uGUI 특성 패널 작업 시 `SynergyManager.GetAllSynergyStatuses()` 그대로 pull하시면 됩니다(현재 IMGUI는 임시). 효과 스키마는 `docs/HANDOFF_2026-06-25_synergy-effect-schema.md` 참고.

## 프로토타입 현황
코어 루프(라운드→쇼핑→전투→보상→반복→승패)는 한 바퀴 돌아갑니다. 남은 핵심 결손은 **레벨업/기물 수 제한**과 **증강 3택1 트리거**(적용 엔진은 이미 있고 제공 흐름만 미배선)입니다.
