# DevLog — 2026-07-13 (영욱)

## 오늘 한 작업

### 1. PR #38 (스킬 기획 확정값 + matchId GUID) 검증 플레이 → 머지 완료
검증 3항목 전부 통과했습니다. master 최신: `8eb06aeb`.

| 항목 | 결과 |
|---|---|
| 마나 초당 10 | ✅ 시전 주기로 확인 (마나 30 유닛=3초, 60대 유닛=6초 주기) |
| 날따름(TAUNT) | ✅ 시전·시전자 중심 r4 타겟팅·지속시간(3성 3.92s) 로그 확인 |
| 2인 matchId 일치 | ✅ ParrelSync 2인 실플레이 — 양쪽 `matches.jsonl`에 동일 GUID + self/partner 교차 기록 정합 |

### 2. 날따름 검증 중 발견 — CC/지원 스킬 시전 로그 공백 (a8eee7cc)
날따름이 "작동 안 하는 것처럼" 보였는데, 실제 원인은 **`CastSkill`이 데미지 스킬만 시전 로그를 찍고 CC/지원 스킬은 로그 없이 return**하고 있던 것이었습니다. VFX도 없는 상태라(VfxDatabase.asset 미생성) 시전 여부를 확인할 방법이 전혀 없었습니다.
- `ApplyCcOrSupportSkill`에 시전 로그 추가 — Taunt는 지속시간도 함께 출력합니다
- 교훈: **전투 메커니즘 검증은 콘솔 로그 기준으로 하고, 시각 QA는 모델/VFX가 붙은 뒤 별도 트랙**으로 봐야 합니다. (도발 타겟 전환→복귀의 눈 확인은 QA 보류 항목으로 남김)

### 3. 파치리스 도발 증강 디버그 버튼 3종 (4e292887)
증강 시스템(해인님) 연결 전까지 날따름의 유일한 검증 수단입니다 — 이브이 디버그와 같은 패턴.
- ③ 파치리스 벤치 지급 (골드/풀 미차감) ④ 도발 증강 적용 (탱커 전환 + 날따름 주입, **마나 30은 검증용 PLACEHOLDER** — 기획 확정 시 교체) ⑤ 보드/벤치 유닛 스킬 상태 콘솔 덤프

### 4. 2인 실테스트에서 알게 된 것 / 경미한 후속 2건
- **ParrelSync 한계**: 클론이 PlayerPrefs를 원본과 공유해 **두 인스턴스가 같은 익명 유저로 Supabase에 업로드**됩니다. `unique(user_id, match_id)` 때문에 서버엔 1행만 남습니다. 실제 2대(다른 기기) 테스트에서는 정상적으로 2행 기록됩니다 — 버그 아님.
- 후속 1: 판 종료가 라운드 전환과 겹치면 **양쪽 전적의 finalRound가 1 차이** 날 수 있음 (마스터 1-4 vs 파트너 1-3으로 기록된 사례)
- 후속 2: 전적의 **partner.level이 항상 0** — 파트너 레벨은 동기화되지 않고 있음 (보드 스냅샷은 정상)

### 5. 기타
- 패널/카드 등급 스타일링 설계 문서 `Docs/DESIGN_2026-07-10_panel-ui-tier-styling.md` master 커밋 (Notion에는 7/10 게시됨)
- 주간회의 보고(7/13) Notion 게시 완료
- 매일 아침 9시 작업 일정 구글 캘린더 자동 등록 루틴 셋업 (개인 도구)

---

# (오후 추가)

### 6. VfxDatabase.asset 생성 (0e50421c)
`Resources/VfxDatabase.asset`을 생성하고 **vfxId 5종을 빈 슬롯으로 사전 등록**했습니다 (VFX_Mutant_Magician/Tanker/Warrior, VFX_Water_Warrior, VFX_Dragon_Magician).
- 아트는 인스펙터에서 **프리팹만 드래그하면 코드 수정 없이 바로 재생**됩니다 (규약: Notion "VFX 연동 규격 안내")
- 전투 중 `[VfxDatabase] 없음` LogError 스팸이 사라지고, 미연결 vfxId는 id당 1회 경고로 바뀝니다

### 7. LFS 확인 작업 검증 + 실행 절차서 (424c6868)
공유받은 확인 작업(`Assets/_Project/LFS.md`)을 실측으로 교차 검증했습니다.
- **진단 정확**: 문제는 히스토리 속 옛 블롭 (현재 master HEAD는 LFS 정상 — 패턴 매칭 121개 전부 추적 중. "4378개 추적"은 오기)
- 정정 1건: `git lfs migrate`의 `--include` 패턴은 **콤마 뒤 공백 없이** 써야 합니다
- 실행 절차서 `Docs/RUNBOOK_lfs-migration.md` 커밋 — 단계별 체크박스/공지 템플릿/pre-commit hook 포함, **작업 도중 인수인계 가능**하게 작성. 영욱이 착수하고 필요 시 이어받는 구조입니다

### 8. 시너지 확정 테이블(CSV) 대조 — 신규 작업 없음
공유받은 synergy_table CSV를 코드/데이터와 전수 대조한 결과 **16종 전부 이미 구현·수치 일치**입니다 (`synergy_data.json` 티어 카운트, `SynergyConstants` 수치, 얼음/악/치어리더/돌연변이 특수 로직 포함). 남은 것은 실플레이 검증뿐입니다.

### 9. 1R 시작골드 실확인
1라운드 시작 골드 **20 확인** (씬 `_startingGold=10` + RW001 보상 10). 7/8 결정("그대로 유지")대로 동작 — 이 항목 종결.

### 10. 발표(8월 말) 역산 일정표 작성
아트/코드/인수인계 마일스톤 역산표를 Notion에 게시했습니다 — **아트 1차 7/20(VFX 5종+견본덱 모델) / 3차 8/14(풀 45종) / 8/22 아트 컷오프**. 선행 조건은 견본덱 리스트 확정(기획)입니다.

## 다음 작업
- 2인 실테스트 잔여 항목 (재접속 / 통신교환 / 증강 3택1 / 골드 전송)
- 견본덱(목표 조합 표시) 기능 — 기획 리스트 확정 대기, 질문 2건 (진화 라인 인정 기준 / 풀 45종 축소와 연동)
- LFS 마이그레이션 착수 (팀 공지 → RUNBOOK 순서대로)
- 시너지 실플레이 검증 (전기 2셋 / 물 3셋 / 돌연변이 2셋)
- vfxId 5종 아트 프리팹 연결 (아트)
